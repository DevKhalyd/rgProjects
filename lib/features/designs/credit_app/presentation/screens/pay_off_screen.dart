import 'package:flutter/material.dart';

// Part of credit app
class PayOffScreen extends StatelessWidget {
  const PayOffScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
