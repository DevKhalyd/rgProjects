import 'package:flutter/material.dart';
import '../../home/screens/home_section.dart';

class NativesServicesSection extends StatelessWidget {
  const NativesServicesSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return HomeSection(title: 'Natives Services');
  }
}
