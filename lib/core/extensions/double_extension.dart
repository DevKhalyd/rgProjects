extension DoubleExtension on double {

  /// Return the percentage of two values
  double getPercentage(double min, double max) {
    final number = this;

    assert(number >= min && number <= max);

    if (number == min) {}

    throw UnimplementedError('Missing return');
  }
}
